package com.example.ccl3_ws2025_mindflow.ui.theme

import androidx.compose.ui.graphics.Color

object MindFlowColors {
    // Background gradient
    val BgTop = Color(0xFFCFEFF2)      // light aqua
    val BgMid = Color(0xFF9EDBE0)      // aqua
    val BgBottom = Color(0xFF7CC8CF)   // deeper aqua

    // Decorative “hills”
    val HillDark = Color(0xFF2B6B61)
    val HillMid = Color(0xFF3C877C)
    val HillLight = Color(0xFF86B78D)

    // Surfaces
    val Surface = Color(0xFFFFFFFF)
    val SurfaceAlt = Color(0xFFF7FAFA)

    // Text
    val TextPrimary = Color(0xFF0E1B1A)
    val TextSecondary = Color(0xFF506362)
    val TextMuted = Color(0xFF8A9A99)

    // Brand
    val Primary = Color(0xFF2B6B61)     // teal (buttons/FAB)
    val OnPrimary = Color(0xFFFFFFFF)

    // Lines / strokes
    val Stroke = Color(0xFFE3E8E8)
    val StrokeStrong = Color(0xFF0E1B1A)

    // Status
    val Success = Color(0xFF5BE06E)     // progress end
    val Danger = Color(0xFFE74C3C)
    val OverlayScrim = Color(0xB3000000)
}
